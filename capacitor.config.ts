import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.wildbelugadash',
  appName: 'wild-beluga-dash',
  webDir: 'dist'
};

export default config;
